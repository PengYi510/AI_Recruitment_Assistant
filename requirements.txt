# Harness-Driven Multimodal Hierarchical Fusion Intelligent Recruitment Matching System
# Runtime Dependencies (运行依赖)
#
# 文本特征: BAAI/bge-m3 模型（1024维，567M参数，~2.2GB）
#   首次使用需从 HuggingFace 下载放到 models/bge-m3/ 目录，详见 README.md
# 图像特征: 真实 BLIP-base 视觉编码器（Salesforce/blip-image-captioning-base）
#   模型缺失时回退为 hash-seeded 确定性向量。详见 README.md 和论文第8.2节。
# 结构化匹配: 首选 CatBoost，未安装时自动回退为 scikit-learn GradientBoosting。
#
# 推荐 Python 3.12 / 3.13。注: Python 3.14 暂无 catboost wheel，
# 此时跳过 catboost 即可，系统会自动使用 sklearn GradientBoosting。
#
# 本文件给出经验证可用的【最低版本】约束（>=），便于在不同 Python 小版本上安装。
# 若需【完全可复现】的精确版本组合（论文实验所用环境），请改用 requirements-lock.txt。
# 开发/测试依赖请见 requirements-dev.txt。
#
# 经实测可正常运行的版本已在每行末尾以 "# tested: x.y.z" 标注（Python 3.14 环境）。

# LLM & API
openai>=1.12.0          # tested: 2.40.0

# Web Framework
flask>=3.0.0            # tested: 3.1.3
flask-cors>=4.0.0
fastapi>=0.109.0        # tested: 0.136.3
uvicorn>=0.27.0         # tested: 0.48.0
requests>=2.31.0        # tested: 2.34.2
httpx>=0.27.0           # tested: 0.28.1

# Multi-Agent Orchestration (Planner-Generator-Evaluator)
langgraph>=0.2.0        # tested: 1.2.4

# Database
# SQLite 3.45.0 (built-in with Python 3.12+)
chromadb>=0.5.11        # tested: 1.5.9
redis>=5.0.0            # tested: 8.0.0  (会话状态存储，支持降级为内存)

# Machine Learning (结构化匹配)
numpy>=1.26.0           # tested: 2.4.6
scikit-learn>=1.4.0     # tested: 1.9.0
scipy>=1.11.0           # tested: 1.17.1

# 可选: CatBoost 结构化匹配器（首选）。未安装时自动回退 sklearn GradientBoosting。
# Python 3.13 需 catboost>=1.2.7；Python 3.14 暂无 wheel，可跳过本行。
# 论文第6节 12 维结构化特征实验在 Python 3.13 + catboost 1.2.7 下完成；
# 在 Python 3.14 环境中系统自动回退至 sklearn GradientBoosting，指标基本一致。
# catboost>=1.2.7

# Retrieval
rank_bm25>=0.2.2        # tested: 0.2.2
jieba>=0.42.1          # tested: 0.42.1

# Vector & Embedding (BGE-M3 文本向量)
sentence-transformers>=2.5.0   # tested: 5.5.1
torch>=2.0.0                   # tested: 2.12.0

# Multimodal Image Encoding (真实 BLIP-base 视觉编码 + 证件图像生成)
# 注意: transformers 5.x 与 BLIP-3-7B(xGen-MM) 官方远程代码(基于 transformers 4.41)
#       存在配置类注册不兼容(XGenMMConfig 无法被 AutoModel 识别)，
#       因此系统实际后端为 BLIP-base。详见论文第 8.2 节。
transformers>=4.38.0   # tested: 5.11.0
torchvision>=0.15.0    # tested: 0.27.0
accelerate>=0.27.0
safetensors>=0.4.0     # tested: 0.8.0
pillow>=10.0.0         # tested: 12.2.0
tokenizers>=0.20.0     # tested: 0.22.2

# Visualization & Explainability (SHAP)
matplotlib>=3.8.0      # tested: 3.10.9
shap>=0.44.0           # tested: 0.52.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0        # tested: 2.13.4
